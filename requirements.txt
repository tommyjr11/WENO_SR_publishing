# Minimal runtime dependencies. CUDA-specific PyTorch wheels may require the
# installation command published for the target CUDA toolkit.
numpy>=2.0
torch>=2.4
warp-lang>=1.6
matplotlib>=3.8
scipy>=1.12
sympy>=1.12
